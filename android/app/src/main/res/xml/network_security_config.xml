<?xml version="1.0" encoding="utf-8"?>
<!--
  The backend base URL is an on-device, admin-configured Settings value (see SettingsScreen),
  not attacker-controlled input. HTTPS is strongly recommended and enforced by default; cleartext
  (http://) is only permitted for local/LAN IP addresses so staff can point a debug build at a
  backend running on a laptop/local network during setup and testing. A production deployment
  should always configure an https:// Base URL.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
